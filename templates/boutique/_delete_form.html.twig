<form method="post" action="{{ path('app_boutique_delete', {'idBo': boutique.idBo}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boutique.idBo) }}">
    <button class="btn">Delete</button>
</form>
