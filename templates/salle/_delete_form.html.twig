<form method="post" action="{{ path('app_salle_delete', {'nbre_salle': salle.getNbreSalle()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle.getNbreSalle()) }}">
    <button class="btn">Delete</button>
</form>
