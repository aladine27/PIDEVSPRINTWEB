{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <div class ="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">
   <div class="col-lg-6"> 
   <div class="card">
  <div class="card-body">
    <h1>SALLE</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Nbre_salle</th>
                <td>{{ salle.nbreSalle }}</td>
            </tr>
            <tr>
                <th>Type_stuff</th>
                <td>{{ salle.typeStuff }}</td>
            </tr>
            <tr>
                <th>Cin_stuff</th>
                <td>{{ salle.cinStuff }}</td>
            </tr>
        </tbody>
    </table>

     <a href="{{ path('app_salle_index') }}">back to list</a>

    <a href="{{ path('app_salle_edit', {'nbre_salle': salle.getNbreSalle()}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
    
            </div>
    </div>
    </div>
 </div>
{% endblock %}
