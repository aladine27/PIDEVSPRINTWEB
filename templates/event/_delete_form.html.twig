<form method="post" action="{{ path('app_event_delete', {'nom_event': event.getNomEvent()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.getNomevent()) }}">
    <button class="btn">Delete</button>
</form>
